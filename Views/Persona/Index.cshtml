@model IEnumerable<GruppoStoricoApp.Models.Persona>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Lista di @ViewData["RuoliLista"]</h1>

<p>
    <a asp-action="Create" asp-route-ruoloId="@ViewData["RuoloId"]">Crea Nuovo @ViewData["Ruolo"]</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cognome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataNascita)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Profilo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ruolo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Cognome)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataNascita)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Profilo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Ruolo.NomeRuolo)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ID">Modifica</a> |
                    <a asp-action="Details" asp-route-id="@item.ID">Dettaglio</a> |
                    <a asp-action="Delete" asp-route-id="@item.ID">Elimina</a>
                </td>
            </tr>
        }
    </tbody>
</table>
